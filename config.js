const dbConfig = {
    connectionLimit: 10,
    host: 'localhost',
    user: 'root',
    password: 'wei',
    database: 'smartnotebook'
}

const appConfig = {
    port: 9001
}

exports = module.exports = {
    dbConfig,
    appConfig
}
